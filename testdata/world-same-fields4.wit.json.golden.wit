package foo:foo

interface shared-items {
    type a = u32
}

world foo {
    import shared-items
    import interface shared-items {}
    export interface a-name {
        use foo:foo/shared-items.{a}
    }
}
