package issues:issue163;

world a {
	variant v-multiple {
		a(list<u8>),
		c(tuple<s32, s32>),
		d(tuple<u32, u32>),
		e(tuple<s64, s64>),
		f(tuple<u64, u64>),
		g(tuple<f32, f32>),
		h(tuple<f64, f64>),
		i(tuple<char, char>),
		j(tuple<bool, bool>),
		k(tuple<s32, list<u8>>),
	}
	import f-multiple: func(x: v-multiple);
	export f-multiple: func(x: v-multiple);
}
