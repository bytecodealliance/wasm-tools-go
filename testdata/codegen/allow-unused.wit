package foo:bar;

world bindings {
    export component;
}

interface component {
    variant unused-variant {
        %string,
        %enum(unused-enum),
        %record(unused-record)
    }
    enum unused-enum {
        unused
    }
    record unused-record {
        x: u32
    }
}
