package examples:resources-in-world;

interface types {
    resource foo {
        bar: func(self: borrow<foo>, arg: u32);
        foo: func(self: borrow<foo>);
    }
}

world imports {
    import types;
}
