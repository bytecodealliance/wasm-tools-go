package example:flat-variant;

interface a {
	variant numbers {
		one,
		two(string),
		three(f64),
		four(option<string>),
	}
	f0: func(f: f64);
	f1: func(v: numbers);
}

world imports {
	import a;
}
