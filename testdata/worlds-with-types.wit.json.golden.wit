package foo:foo

interface disambiguate {
    type t = u32
}

world bar {
    import disambiguate
    use foo:foo/disambiguate.{t}
    export foo: func() -> t
}

world foo {
    type a = u32
    type b = a
    export c: func(a: a) -> b
}

world the-test {
    record a {
        x: u32
    }
    /* TODO(b) */
    import foo: func(a: a) -> b
    export bar: func(a: a) -> b
}
